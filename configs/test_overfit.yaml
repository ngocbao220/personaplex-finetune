seed: 42
model:
  root: ../models # Path đến model personaplex-7b
  # Self-contained vendored runtime package.
  source: ../src # Path đến thư mục chứa code của PersonaPlex
  device: cuda
data:
  prepared_dir: ../prepared # Contains train.jsonl and samples/conv_XXXX
  window_seconds: 30 # Window seconds
  shuffle: false # Shuffle 
lora:
  rank: 16
  alpha: 32
train:
  output_dir: ../runs/hf_overfit_10
  max_steps: 300
  learning_rate: 2.0e-5
