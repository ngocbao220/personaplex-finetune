name: personaplex-overfit
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - ffmpeg
  - pip:
      - numpy>=1.26,<2.2
      - einops==0.7
      - aiohttp>=3.10.5,<3.11
      - PyYAML>=6
      - safetensors>=0.4
      - sentencepiece==0.2
      - sphn>=0.1.4,<0.2
      - sounddevice==0.5
      - pyloudnorm>=0.1.1
      - tqdm>=4.66
